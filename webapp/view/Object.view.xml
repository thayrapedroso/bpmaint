<mvc:View
    controllerName="bpmaint.controller.Object"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:semantic="sap.f.semantic"
    xmlns:form="sap.ui.layout.form"
    xmlns:core="sap.ui.core">

    <semantic:SemanticPage
        id="page"
        headerPinnable="false"
        toggleHeaderOnTitleClick="false"
        busy="{objectView>/busy}"
        busyIndicatorDelay="{objectView>/delay}"
        showFooter="{objectView>/edit}">

        <semantic:content>
            <form:Form id="formBPDetail" width="100%" editable="{objectView>/edit}">
                <form:title>
                    <core:Title text="{i18n>txtBPDetail}" level="Auto"/>
                </form:title>
                <form:layout>
                    <form:ResponsiveGridLayout/>
                </form:layout>
                <form:formContainers>
                    <form:FormContainer title="{i18n>txtCentralData}">
                        <form:FormElement label="{i18n>txtPartnerId}">
                            <form:fields> 
                                <Label text="{PartnerId}" />
                            </form:fields>
                        </form:FormElement>
                        <form:FormElement label="{i18n>txtPartnerType}">
                            <form:fields> 
                                <Input value="{PartnerType}" editable="{objectView>/edit}"/>
                            </form:fields>
                        </form:FormElement>
                        <form:FormElement label="{i18n>txtPartnerName}">
                            <form:fields> 
                                <Input value="{PartnerName1}" editable="{objectView>/edit}"/> 
                                <Input value="{PartnerName2}" editable="{objectView>/edit}"/>
                            </form:fields>
                        </form:FormElement>
                        <form:FormElement label="{i18n>txtSearchTerm}">
                            <form:fields> 
                                <Input value="{SearchTerm1}" editable="{objectView>/edit}"/> 
                                <Input value="{SearchTerm2}" editable="{objectView>/edit}"/>
                            </form:fields>
                        </form:FormElement>
                    </form:FormContainer>
                    <form:FormContainer title="{i18n>txtAddressData}">
                        <form:FormElement label="{i18n>txtStreet}">
                            <form:fields> 
                                <Input value="{Street}" editable="{objectView>/edit}"/>
                            </form:fields>
                        </form:FormElement>
                        <form:FormElement label="{i18n>txtHouseNumber}">
                            <form:fields> 
                                <Input value="{HouseNumber}" editable="{objectView>/edit}"/>
                            </form:fields>
                        </form:FormElement>
                        <form:FormElement label="{i18n>txtDistrict}">
                            <form:fields> 
                                <Input value="{District}" editable="{objectView>/edit}"/>
                            </form:fields>
                        </form:FormElement>
                        <form:FormElement label="{i18n>txtCity}">
                            <form:fields> 
                                <Input value="{City}" editable="{objectView>/edit}"/>
                            </form:fields>
                        </form:FormElement>
                        <form:FormElement label="{i18n>txtRegion}">
                            <form:fields> 
                                <Input value="{Region}" editable="{objectView>/edit}"/>
                            </form:fields>
                        </form:FormElement>
                        <form:FormElement label="{i18n>txtZipCode}">
                            <form:fields> 
                                <Input value="{ZipCode}" editable="{objectView>/edit}"/>
                            </form:fields>
                        </form:FormElement>
                        <form:FormElement label="{i18n>txtCountry}">
                            <form:fields> 
                                <Input value="{Country}" editable="{objectView>/edit}"/>
                            </form:fields>
                        </form:FormElement>
                    </form:FormContainer>
                </form:formContainers>
            </form:Form>
		</semantic:content>

        <semantic:titleMainAction>
			<semantic:TitleMainAction text="{i18n>btnEdit}" press="onEditPress" />
		</semantic:titleMainAction>

        <!-- Custom Footer Content-->
		<semantic:footerCustomActions>
			<Button text="{i18n>btnSave}" press="onSavePress" />
			<Button text="{i18n>btnCancel}" press="onCancelPress" />
		</semantic:footerCustomActions>


    </semantic:SemanticPage>

</mvc:View>